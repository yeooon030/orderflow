## 0. 최우선 지시 — 작업 시작 전 필독

이 파일의 다른 내용을 적용하기 전에 `docs/.internal/HANDOVER.md`를 읽는다.

```text
docs/.internal/HANDOVER.md    # 항상 읽는다. 직전 작업 상태와 다음 할 일
```

나머지 두 파일은 항상 읽지 않는다. 필요할 때만 읽는다.

```text
docs/.internal/PROGRESS.md    # 과거 결정과 그 근거, 누적 기록을 확인할 때
docs/.internal/CHECKLIST.md   # 항목을 확인하거나 완료 표시할 때. 현재 Day 섹션만 읽는다
```

세션마다 전체를 읽으면 토큰 소모가 크고, 두 파일은 계속 늘어난다.
작업을 시작하는 데 필요한 내용은 `HANDOVER.md`에 있어야 한다.

- `HANDOVER.md`를 읽고 현재 Phase를 확인한 뒤 작업을 시작한다.
- `HANDOVER.md`가 없으면 일정 제약이 없는 것으로 간주하지 않는다.
  `PROGRESS.md`와 `git log`로 직전 상황을 파악하고 사용자에게 확인을 요청한다.
- `HANDOVER.md`의 내용이 현재 저장소 상태와 다르면 저장소를 기준으로 하고,
  차이를 사용자에게 알린다.

`docs/.internal/` 경로는 저장소에 커밋하지 않는다.
개발 기간, 일정, 진행 상황을 다른 문서나 커밋 대상 파일에 옮겨 적지 않는다.

단, 기술적 의사결정은 판단 기준에 해당하면 `docs/decisions/`에 작성한다.
기준과 시점은 DEVELOPMENT_RULES.md 20을 따른다.

## 0-1. 세션 인수인계

`docs/.internal/HANDOVER.md`는 다음 세션이 이어서 작업할 수 있도록 남기는 문서다.

**다음 세션은 이 파일만 읽고 작업을 시작한다.**
`PROGRESS.md`와 `CHECKLIST.md`는 필요할 때만 읽으므로,
작업 재개에 필요한 내용이 빠지면 맥락 없이 시작하게 된다.

### 세션 시작

`HANDOVER.md`가 있으면 가장 먼저 읽는다.
직전 작업 내용과 다음 할 일을 사용자에게 먼저 요약한 뒤 이번 지시를 처리한다.

파일이 없으면 `PROGRESS.md`와 `git log`로 직전 상황을 파악한다.

### 세션 종료

사용자가 작업 종료, 마무리, 인수인계 의사를 밝히면 `HANDOVER.md`를 새로 쓴다.

항상 덮어쓴다. 최신 상태 한 장만 유지한다.

```markdown
# 인수인계 — YYYY-MM-DD

## 직전 세션에서 한 일

## 다음에 할 일

## 미해결 이슈 / 결정 대기

## 현재 상태
- 브랜치 / 원격 동기화 여부
- 커밋되지 않은 변경
- 테스트 결과
- Docker 컨테이너 상태

## 재개 절차
```

작성 후 다음을 확인한다. 하나라도 답할 수 없으면 그 내용을 추가한다.

- 지금 어느 Phase이고 다음에 무엇을 하는가
- 진행을 막는 이슈가 있는가
- 커밋되지 않은 변경이 있는가. 테스트는 통과 상태인가
- 다음 작업에서 반드시 지켜야 할 제약이 있는가

`PROGRESS.md`가 누적 기록이라면 `HANDOVER.md`는 현재 시점의 상태다.
같은 내용을 양쪽에 중복해서 쓰지 않는다.

과거 결정의 근거는 `PROGRESS.md`나 `docs/decisions/`에 두고,
`HANDOVER.md`에는 다음 작업에 영향을 주는 것만 남긴다.
이 파일이 계속 커지면 축소한 의미가 없다.

## 0-2. 브랜치

문서만 변경하는 작업은 브랜치를 만들지 않고 main에서 직접 작업한다.

대상: `docs/` 하위 문서, `CLAUDE.md`, `README.md`

코드 변경은 `feature/*` 브랜치에서 작업한다.
코드와 문서를 함께 변경하면 코드 기준으로 본다.

## 1. 프로젝트 기준

이 프로젝트의 요구사항과 개발 기준은 다음 문서를 따른다.

0. docs/.internal/HANDOVER.md — 직전 작업 인수인계 (커밋 제외, 항상 읽는다)
1. docs/.internal/PROGRESS.md — 개발 기간, 일정, 누적 기록 (커밋 제외, 필요할 때만)
2. docs/.internal/CHECKLIST.md — 일별 작업 체크리스트 (커밋 제외, 필요할 때만)
3. docs/PROJECT.md — 프로젝트 목표, 범위
4. docs/BACKEND.md — 백엔드 기능 및 기술 명세
5. docs/DEVELOPMENT_RULES.md — 개발 및 테스트 규칙
6. CLAUDE.md — Claude Code의 작업 방식과 행동 규칙

문서 간 충돌이 발생하면 임의로 판단하지 않는다.
충돌 내용을 사용자에게 보고하고 확인을 요청한다.

## 2. 작업 방식

한 번에 전체 프로젝트를 구현하지 않는다.

항상 다음 순서로 작업한다.

1. 현재 상태 확인
2. 요구사항 확인
3. 구현 계획 제시
4. 사용자 승인
5. 구현
6. 테스트
7. 결과 검증
8. 변경 사항 요약


## 3. 범위 통제

현재 Phase의 작업만 수행한다.

다음 Phase의 기능을 미리 구현하지 않는다.

요구사항에 없는 기능을 임의로 추가하지 않는다.

새로운 라이브러리, 프레임워크, 인프라 기술을 추가하기 전에
사용자에게 필요성과 대안을 설명하고 승인을 받는다.

단, 기존 기술 스택으로 해결 가능한 문제에 새로운 의존성을 추가하지 않는다.


## 4. 코드 작성 원칙

기존 코드를 먼저 확인한다.

새로운 구조를 만들기 전에 현재 프로젝트 구조와 기존 구현을 확인한다.

불필요한 추상화와 디자인 패턴을 추가하지 않는다.

`docs/.internal/PROGRESS.md`에 기록된 일정 제약을 항상 고려한다.


## 5. 테스트

기능 구현 후 관련 테스트를 작성하고 실행한다.

테스트가 실패하면 원인을 분석하고 수정한다.

구현 관련 테스트가 실패한 상태에서는 다음 구현 작업으로 넘어가지 않는다.
단, 문서 작성이나 사용자의 별도 지시가 있는 경우는 예외로 한다.


## 6. 성능

성능 문제는 추측하지 않는다.

반드시 측정 → 분석 → 개선 → 재측정 순서로 진행한다.

실제 측정 결과가 없는 성능 개선 효과를 주장하지 않는다.


## 7. 동시성

재고 차감은 Pessimistic Lock을 사용한다.

Optimistic Lock을 별도로 구현하거나 비교하지 않는다.

동시성 테스트는 실제 여러 DB Connection을 사용하여 검증한다.

Lock 적용 여부는 코드만으로 판단하지 않고 동시성 테스트를 통해 검증한다.


## 8. 데이터

데이터 규모는 다음 기준을 따른다.

SMALL = 10K
MEDIUM = 100K
LARGE = 1M

개발 중에는 SMALL을 사용한다.

성능 분석은 MEDIUM을 사용한다.

LARGE는 최종 검증에서만 사용한다.


## 9. AI 활용

Claude가 작성한 코드를 그대로 신뢰하지 않는다.

생성된 코드는 기존 요구사항, 코드 구조, 테스트 결과를 기준으로 검토한다.

AI가 제안한 기술적 선택과 실제 적용 결과가 다른 경우 그 차이와 원인을 기록한다.

Claude가 제안한 해결 방법을 그대로 정답으로 간주하지 않는다.

중요한 기술적 결정은 다음 과정을 따른다.

문제 발견
→ Claude 분석
→ 해결안 검토
→ 실제 실행
→ 결과 확인
→ 최종 결정

AI의 답변보다 실제 테스트와 측정 결과를 우선한다.


## 10. 문서화

단순한 구현 변경은 기록하지 않는다.

의사결정은 두 곳에 나누어 기록한다. 판단 기준, 작성 시점, 문서 형식은
DEVELOPMENT_RULES.md 20을 따른다.

| 위치 | 대상 | 커밋 |
|---|---|---|
| `docs/decisions/` | 추후 학습에 도움이 되는 결정 | O |
| `docs/.internal/PROGRESS.md` | 그 외 나머지 결정 | X |

`docs/decisions/`에 기록할지는 다음으로 판단한다.

- 다른 프로젝트나 다른 상황에도 적용할 수 있는가
- 성능 또는 안정성에 영향을 주는가
- 측정하거나 재현해서 확인한 결과가 있는가

문서는 구현과 검증이 끝나 실제 결과가 나온 뒤에 작성한다.
결과가 없는 결정은 PROGRESS.md에 두고, 결과가 나온 시점에 docs/decisions/로 옮긴다.
Day 14에 몰아서 작성하지 않는다.

커밋 대상 문서에는 개발 기간과 일정을 작성하지 않는다.
일정 관련 내용은 `docs/.internal/PROGRESS.md`에만 기록한다.


## 11. 기존 코드 변경

기존 코드 수정이 필요한 경우 수정 이유와 영향을 먼저 확인한다.

현재 작업과 직접 관련 없는 코드의 리팩토링이나 구조 변경을 하지 않는다.

"더 좋은 구조"라는 이유만으로 기존 코드를 임의로 변경하지 않는다.

## 12. 작업 완료 보고

각 작업이 끝나면 다음 형식으로 보고한다.

### 변경 사항
- ...

### 테스트
- 실행한 테스트
- 결과

### 검증
- ...

### 결정 사항
- ...

### 다음 작업
- ...

사용자의 추가 지시 없이 다음 Phase로 자동 진행하지 않는다.